# DESIGN.md — TensorLogix: DEEP DARK CMS (премиальный тёмный стиль)

> v2.0 от 24.08.2026. LIGHT MATRIX АННУЛИРОВАН. Единая дизайн-система: сайт + TMA + админка.

## 1. Общая стилистика
- Глубокий тёмный фон (Deep Dark Mode): базовый `#04060b`, панели `#0a0f1a`.
- Неоновая подсветка элементов: зелёный `#22CE71` и синий `#0EA5E9` (градиенты свечения).
- Хромированные и стальные текстуры: полированные градиенты `#f8fafc→#94a3b8→#e2e8f0→#475569`
  для текста-хрома и стальных рамок `rgba(148,163,184,.25)`.
- Фирменный знак: удлинённая бипирамида (зелёная/синяя грани) — ядро визуала.

## 2. Типографика
- Текст: системный стек Inter (.clinerules). Дисплей/бренд: Space Grotesk (допустим Orbitron).
- Брендовый блок хедера: логотип и словознак ОДИНАКОВОЙ высоты = 5 строк базового
  текста (≈80px desktop, 48px mobile), словознак встык с логотипом, uppercase, трекинг.

## 3. Композиция лендинга (блоки 0–4)
0. Хедер — постоянная фиксация брендового блока.
1. Hero: слева неоновый текст (заголовок/подзаголовок/слоган в квадратных скобках),
   справа `BipyramidCore` + лучи `tlx-beam` к карточке «Терминал управления»; CTA-скролл.
2. Хаб экосистемы: 5 вкладок, индикатор layoutId, панели «Боли/Решение/Этапы».
3. Калькулятор v2: чекбоксы-карточки с неоновой рамкой при выборе; табло итогов
   с анимированными цифрами; CTA к подвалу; BottomSheet в мобильном/TMA-режиме.
4. Подвал: форма захвата + оферта + контакты.

## 4. Движение — только Framer Motion + Canvas скилла
- Вкладки и панели: spring-переключение (layoutId, AnimatePresence).
- Табло сметы: анимированная смена цифр.
- Пирамида: Canvas-анимация скилла; реакция на курсор; prefers-reduced-motion.
- Лучи: CSS-анимация штриха `tlx-dash`.

## 5. Анти-слоп
- Запрещены: светлые темы, glassmorphism по умолчанию, карточки-в-карточках,
  бессмысленные чипы. Каждый компонент: `impeccable detect` + `tsc --noEmit` + скриншот-регрессия.
