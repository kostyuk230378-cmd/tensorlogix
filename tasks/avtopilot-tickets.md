# АВТОПИЛОТ — тикеты по ТЗ v1.0 (от 24.08.2026)

Роли: `/swe-agent` пишет, `/test-driven` лечит + `impeccable detect` + Playwright-регрессия,
`/refactor` чистит, `/doc-gen` документирует, `/git-flow` коммитит.
Фронтенд — строго ОДИН компонент за цикл со СТОП-ТОЧКОЙ (скриншот в чат → ждём одобрения).

## Бэкенд (автономно)
- [x] BACK-1: модель `Lead` в `prisma/schema.prisma` (конфигурация калькулятора, маркетинг, смета, контакты, статусы)
- [ ] BACK-2: накатить схему в Supabase (`npx prisma db push`) — **БЛОКЕР: БД недоступна (P1001), ждём Restore проекта заказчиком**
- [x] BACK-3: `POST /api/leads` — Zod + бизнес-правила динамических блоков (ТЗ §2) + запись в `leads`
- [x] BACK-4: Telegram-уведомления о новых лидах (`TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` в `.env`)
- [ ] BACK-5: `GET /api/leads` с фильтрами для CRM-таблицы админки

## Фронтенд (интерактивно, по одному компоненту со стоп-точкой)
- [ ] UI-01: каркас `(site)` + LIGHT MATRIX фон (цифровой дождь на белом, канвас) + логотип
- [ ] UI-02: калькулятор, блок 1 — тип сайта (5 радио-карточек, Framer Motion)
- [ ] UI-03: блок 2 — интеграции сайта (динамическое раскрытие)
- [ ] UI-04: блок 3 — toggle TMA + блок 4 — интеграции Mini App (условное раскрытие)
- [ ] UI-05: блок 5 — модуль управления + чекбоксы маркетинга + живой пересчёт сметы/сроков
- [ ] UI-06: симулятор — вкладки «Дизайн Сайта» / «Дизайн Mini App» (3 стиля, плавная смена тем макетов)
- [ ] UI-07: симулятор — «Терминал управления (Демо-Админка)»: Drag-and-Drop карточки/инпуты → мгновенная синхронизация макетов
- [ ] UI-08: фиксация сметы + форма захвата (Имя/Телефон-ТГ/Email → `POST /api/leads`) + оферта + блок прямых контактов
- [ ] UI-09: `(admin)` CRM-таблица лидов с фильтрами
- [ ] UI-10: `(tma)` мини-апп-демонстратор собственного продукта

## Открытые вопросы к заказчику (блокируют соответствующие тикеты)
1. **Ценовая сетка калькулятора** — сейчас в `src/lib/calculator/pricing.ts` плейсхолдеры (помечены).
2. **Контакты-заглушки владельца** — телефон / telegram-юзернейм / email для блока прямых контактов.
3. **TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID** — заполнить `.env` (токен у @BotFather).
4. **Restore проекта Supabase** — без него таблицы не развернуть (P1001).